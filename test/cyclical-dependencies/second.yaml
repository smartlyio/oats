openapi: 3.0.0
info:
  version: 1.0.0
  title: fixtures for runtime test
paths: {}
components:
  schemas:
    SecondObject:
      type: object
      properties:
        target:
          $ref: './first.yaml#/components/schemas/FirstObject'
